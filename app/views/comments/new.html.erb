<%= form_with model: Comment.new, local: true do |form|%>
  <div class="field">
    <%= form.label :comment_text %>
    <%= form.rich_text_area :text %>
  </div>

  <%= form.submit "Create" %>
<% end %>